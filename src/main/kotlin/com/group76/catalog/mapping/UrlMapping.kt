package com.group76.catalog.mapping

object UrlMapping {
    object Version {
        const val V1 = "/v1"
    }

    object Resource {
        const val PRODUCT = "/product"
        const val PRODUCT_TEST = "/product-test"
    }
}